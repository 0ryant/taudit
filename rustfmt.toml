# rustfmt — deterministic formatting across machines.
# Stable options only; nightly-only knobs are intentionally omitted.

edition = "2021"
tab_spaces = 4
hard_tabs = false
newline_style = "Unix"
use_field_init_shorthand = true
use_try_shorthand = true
