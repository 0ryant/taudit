# Clean workflow — should produce zero or minimal findings
name: CI
on: push

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@a5ac7e51b41094c92402da3b24376905380afc29
      - name: Run tests
        run: cargo test
