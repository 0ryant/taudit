[package]
name = "taudit"
description = "CI/CD authority graph analyzer: how credentials, tokens, and artifacts propagate across pipelines—implicit trust-boundary breaks and non-obvious privilege escalation. Graph-first; not a YAML linter, generic scanner, or policy engine."
# Product version line (tags v1.1.x, CHANGELOG v1.2.0-rc.1 -> 1.3.x-pre).
# Library crates version independently on their own semver-honest line
# (taudit-core et al. at 3.x) — do NOT "align" this to the workspace libs:
# `taudit update` compares this against the published crates.io 1.x line,
# and a 3.x stamp here makes every future 1.x release look older.
version = "1.3.3"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
documentation.workspace = true
keywords.workspace = true
categories.workspace = true
readme = "../../README.md"

[[bin]]
name = "taudit"
path = "src/main.rs"

[dependencies]
taudit-core = { version = "3.1.0", path = "../taudit-core" }
taudit-parse-gha = { version = "3.1.0", path = "../taudit-parse-gha" }
taudit-parse-ado = { version = "3.1.0", path = "../taudit-parse-ado" }
taudit-parse-gitlab = { version = "3.1.0", path = "../taudit-parse-gitlab" }
taudit-parse-bitbucket = { version = "3.1.0", path = "../taudit-parse-bitbucket" }
taudit-report-terminal = { version = "3.1.0", path = "../taudit-report-terminal" }
taudit-report-json = { version = "3.1.0", path = "../taudit-report-json" }
taudit-report-sarif = { version = "3.1.0", path = "../taudit-report-sarif" }
taudit-sink-cloudevents = { version = "3.1.0", path = "../taudit-sink-cloudevents" }
chrono = { workspace = true }
clap = { workspace = true }
clap_complete = { workspace = true }
anyhow = { workspace = true }
serde_yaml = { workspace = true }
serde_json = { workspace = true }
serde = { workspace = true }
blake3 = { workspace = true }
colored = { workspace = true }
ureq = { workspace = true }
semver = { workspace = true }
axiom-exit = { workspace = true }
axiom-hash = { workspace = true }
axiom-audit = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
serde_yaml = { workspace = true }
jsonschema = { workspace = true }
insta = { version = "1.43", features = ["yaml"] }
